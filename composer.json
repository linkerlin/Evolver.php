{
    "name": "linkerlin/evolver-php",
    "description": "🧬 Capability Evolver PHP implementation - MCP stdio server with SQLite storage",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "Evolver\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Evolver\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit --testdox"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    }
}
